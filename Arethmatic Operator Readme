In the following program we used to do the operation using the arethmatic operator.
In the code we used the two integer value to perform function.
In the code we used + operator , - operator , * operator and / operator to carry the function like addition , substraction ,multiplication and division.
And used to COUT function to execute the operator.
